import React from 'react'
import Cards from './Cards'

const Courses = ({filterData, apiUrl}) => {
   
   //console.log(allCourses)
  return (
    <div>
      <Cards></Cards>
    </div>
  )
}

export default Courses
